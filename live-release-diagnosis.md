# TRIHEX Live Release Diagnosis

**Checked:** 13 August 2026 (GMT+5:45)

## Verified comparison

| Surface | Observed state | Conclusion |
| --- | --- | --- |
| GitHub `main` | Head commit `86b2ac8` contains the premium storefront, quote workflow, and owner controls. | Repository is current. |
| Current local build | Navigation reads **Shop / AI tools / AI services / Guides / Deals**. Hero reads **“Premium AI & Digital Tools for Nepal, built to help you move faster.”** The catalogue has clearer delivery cues and 21 current catalogue entries. | Latest implementation is present locally. |
| `https://trihexdigital.shop` | Navigation reads **Products / Inquire list / AI Tools / Blog / Deals**. Hero reads **“Premium AI & Digital Tools for Nepal.”** and serves an older 57-package catalogue. | The live domain is serving an older Vercel deployment, not GitHub `main` at commit `86b2ac8`. |
| GitHub deployments API | Returned no linked GitHub deployment records for `unesh123/TRIHEX`. | A GitHub push is not automatically updating the Vercel production project. |
| User’s hydration screenshot | The reported mismatch adds `__processed_*="true"` to `<body>`, a non-TRIHEX attribute injected before hydration. | The screenshot’s hydration overlay is consistent with browser-extension DOM mutation; it is separate from the stale production deployment. |

## Deployment facts

The live domain is served by Vercel. This repository has `vercel.json` but no committed `.vercel/project.json` linkage and no authenticated Vercel CLI available in this environment. Therefore production deployment requires linking or authenticating to the actual Vercel project; pushing GitHub commits alone currently cannot publish the release.

## Recommended correction

1. Link the Vercel project to the `unesh123/TRIHEX` GitHub repository and set production branch to `main`, **or** authenticate the Vercel CLI to the current TRIHEX Vercel project and deploy from this repository.
2. Ensure all production environment variables from `PHASE0_PRODUCTION_RUNBOOK.md` exist in the Vercel project before promoting the quote migration-dependent release.
3. Apply `drizzle/0003_quote_workflow.sql` against the production database, then deploy commit `86b2ac8`.
4. In the local browser, test once in Incognito with extensions disabled. The `__processed_*` body attribute must disappear; it is not generated by TRIHEX source.
