import { AccountShell } from "@/components/storefront/account-shell";

export default function AccountPreferencesPage() {
  return (
    <AccountShell
      title="Preferences"
      description="Email and notification preferences require sign-in."
    />
  );
}
